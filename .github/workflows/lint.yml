name: Lint

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  shellcheck:
    name: ShellCheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run ShellCheck on scripts
        uses: ludeeus/action-shellcheck@2.0.0
        with:
          scandir: .
          additional_files: "install.sh uninstall.sh build.sh"
      - name: Run ShellCheck on hook scripts
        uses: ludeeus/action-shellcheck@2.0.0
        with:
          scandir: .claude/hooks/scripts

  markdownlint:
    name: Markdown Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: DavidAnson/markdownlint-cli2-action@v19
        with:
          globs: |
            *.md
            .claude/**/*.md
            templates/**/*.md

  validate-json:
    name: Validate JSON
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate plugin.json
        run: jq empty plugin/.claude-plugin/plugin.json
      - name: Validate marketplace.json
        run: jq empty .claude-plugin/marketplace.json
      - name: Validate settings-hooks.json
        run: jq empty .claude/settings-hooks.json

  test-hooks:
    name: Hook Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run hook tests
        run: bash tests/test-hooks.sh
